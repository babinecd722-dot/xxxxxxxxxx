Exact copies of the six .lua files from ~/Downloads (blast.hk), committed as plain files (not inside a zip).

Note: raksamp/forum_lua_blasthk/uCrashProtector.lua is a small fork: it adds MAX_MENUS / MAX_PLAYERS so the script runs under RakSAMP Lite. The file here is the unmodified original from Downloads.
